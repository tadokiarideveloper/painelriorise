name = "painelriorise"
pages_build_output_dir = "."
compatibility_date = "2026-07-06"

# O banco D1 deve ser vinculado pelo painel da Cloudflare:
# Workers & Pages > seu projeto > Settings > Bindings > Add > D1 database
# Binding/Variable name: DB
# Database: rio-rise-db
#
# Não deixe database_id com texto de exemplo aqui, senão o deploy falha.
